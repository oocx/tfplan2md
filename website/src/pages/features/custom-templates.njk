---
layout: layouts/base.njk
title: "Built-In Templates and Rendering - tfplan2md"
description: "tfplan2md ships with built-in report templates powered by a pure C# rendering engine."
permalink: /features/custom-templates.html
rootPrefix: "../"
navKey: "features"
pageId: "features/custom-templates"
---
<!-- markdownlint-disable MD033 MD032 -->
{% from "components/content-blocks.njk" import heroBlock, featureDetail, relatedDocs, codeWindow %}

{{ heroBlock("Built-In Templates and Rendering", "Choose between the built-in report layouts and rely on the pure C# rendering engine for the rest.", "hero hero-secondary") }}

<section class="section">
  <div class="section-container">
    {% call featureDetail("", "all providers", "tfplan2md now provides built-in `default` and `summary` report modes powered by a pure C# rendering engine. Custom Scriban template files are no longer supported.") %}
      <div class="feature-example">
        <h3 class="example-title">Using the Built-In Modes</h3>
        {{ codeWindow("Command", '<pre><code># Full report with grouped resources, outputs, and details\ntfplan2md plan.json --template default\n\n# Compact overview for notifications and quick triage\ntfplan2md plan.json --template summary</code></pre>') }}
      </div>

      <div class="feature-highlights">
        <h3 class="highlights-title">What Changed</h3>
        <ul class="highlights-list">
          <li><strong>Pure C# rendering:</strong> Rendering logic now lives in statically typed renderer classes instead of runtime template files.</li>
          <li><strong>Built-in modes only:</strong> `default` and `summary` remain the supported user-facing layouts.</li>
          <li><strong>Provider-specific renderers:</strong> Complex resources still get specialized output, but it is implemented directly in the codebase.</li>
          <li><strong>Main CLI footprint:</strong> The main tfplan2md executable now runs with zero third-party runtime dependencies.</li>
        </ul>
      </div>

      <div class="feature-example">
        <h3 class="example-title">Why It Matters</h3>
        {{ codeWindow("Benefits", '<pre><code>- Compile-time checked rendering changes\n- No runtime template resolution failures\n- Cleaner NativeAOT output and smaller shipping artifacts\n- Simpler provider-specific rendering for complex resources</code></pre>') }}
      </div>
    {% endcall %}
  </div>
</section>

<section class="section section-alt">
  <div class="section-container">
    {% call featureDetail("Learn More", "", "tfplan2md includes built-in templates and resource-specific renderers, but user-provided template files are no longer part of the supported surface area.") %}
      {{ relatedDocs("Related Documentation", [
        {"href": "https://github.com/oocx/tfplan2md/tree/main/docs/features/007-built-in-templates", "label": "Feature Specification: Built-in Templates", "external": true},
        {"href": "https://github.com/oocx/tfplan2md/tree/main/docs/features/026-template-rendering-simplification", "label": "Template Rendering Architecture", "external": true},
        {"href": "https://github.com/oocx/tfplan2md/tree/main/docs/features/107-remove-scriban", "label": "Feature 107: Remove Scriban", "external": true},
        {"href": "../docs.html#template-customization", "label": "Templates and Mapping Docs"}
      ]) }}
    {% endcall %}
  </div>
</section>
