## Azure DevOps principal mappings in large attributes

Mapped Azure DevOps group and member descriptors now render as their readable principal names even when the raw descriptor exceeds the large-value threshold.
