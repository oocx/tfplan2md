# Testing Strategy

- **Unit Tests**: Test individual components in isolation using mocking
- **Integration Tests**: Test JSON parsing and markdown generation end-to-end. As the application will be run via docker, use docker to test the final CLI behavior.
