{{ include "_header.sbn" }}

## Summary

{{ if summary.total == 0 }}
No changes
{{ else }}

| Action | Count | Resource Types |
| -------- | ------- | ---------------- |
| ➕ Add | {{ summary.to_add.count }} | {{ for type in summary.to_add.breakdown }}{{ type.count }} {{ type.type | escape_markdown }}{{ if !for.last }}<br/>{{ end }}{{ end }} |
| 🔄 Change | {{ summary.to_change.count }} | {{ for type in summary.to_change.breakdown }}{{ type.count }} {{ type.type | escape_markdown }}{{ if !for.last }}<br/>{{ end }}{{ end }} |
| ♻️ Replace | {{ summary.to_replace.count }} | {{ for type in summary.to_replace.breakdown }}{{ type.count }} {{ type.type | escape_markdown }}{{ if !for.last }}<br/>{{ end }}{{ end }} |
| ❌ Destroy | {{ summary.to_destroy.count }} | {{ for type in summary.to_destroy.breakdown }}{{ type.count }} {{ type.type | escape_markdown }}{{ if !for.last }}<br/>{{ end }}{{ end }} |
| **Total** | **{{ summary.total }}** | |
{{ end }}

{{ include "_code_analysis_summary.sbn" }}

{{ if module_changes.size > 0 }}
## Resource Changes

{{ for module in module_changes }}
{{ if for.index > 0 }}
---

{{ end }}
### 📦 Module: {{ if module.module_address && module.module_address != "" }}{{ format_code_table(module.module_address) }}{{ else }}root{{ end }}

{{ for change in module.changes }}
{{~ # Include the appropriate template - the 'change' variable is accessible in the included template ~}}
{{ include (resolve_template change.type) }}

{{ end }}
{{ if module.outputs && module.outputs.size > 0 }}

#### 📤 Outputs

| Change | Name | Description | Sensitive | Value |
| ------ | ---- | ----------- | --------- | ----- |
{{~ for output in module.outputs ~}}
| {{ output.action_symbol }} | `{{ output.name | escape_markdown_table_cell }}` | {{ if output.description }}{{ output.description | escape_markdown_table_cell }}{{ end }} | {{ if output.is_sensitive }}🔒 Yes{{ else }}No{{ end }} | {{ if output.is_large_output_value }}_(see below)_{{ else }}{{ format_output_value output.is_masked output.is_computed output.value output.provider_name output.formatting_attribute_name }}{{ end }} |
{{~ end ~}}
{{~ for output in module.outputs ~}}
{{~ if output.is_large_output_value ~}}

**`{{ output.name | escape_markdown_table_cell }}`:**

{{ format_output_value_large output.is_masked output.is_computed output.value }}
{{~ end ~}}
{{~ end ~}}
{{ end }}
{{ end }}
{{ end }}

{{ include "_code_analysis_other_findings.sbn" }}

{{ if refactoring_operations.size > 0 }}

## Refactoring Summary

| Operation | Resource | Details | Status |
| --------- | -------- | ------- | ------ |
{{~ for operation in refactoring_operations ~}}
| {{ if operation.operation == "Import" }}📥 Import{{ else }}🔀 Move{{ end }} | {{ operation.resource_type | escape_markdown }} {{ format_code_table(operation.resource_name) }} | {{ if operation.operation == "Import" }}ID: {{ format_import_id_details(operation.details) }}{{ else }}From: {{ format_code_table(operation.details) }}{{ end }} | {{ if operation.is_already_applied }}{{ if operation.operation == "Import" }}⚠️ Already imported{{ else }}⚠️ Already moved{{ end }}{{ else }}✅ Ready{{ end }} |
{{~ end ~}}
{{ end }}

{{ if global_outputs && global_outputs.size > 0 }}

## 📤 Outputs

| Change | Name | Description | Sensitive | Value |
| ------ | ---- | ----------- | --------- | ----- |
{{~ for output in global_outputs ~}}
| {{ output.action_symbol }} | `{{ output.name | escape_markdown_table_cell }}` | {{ if output.description }}{{ output.description | escape_markdown_table_cell }}{{ end }} | {{ if output.is_sensitive }}🔒 Yes{{ else }}No{{ end }} | {{ if output.is_large_output_value }}_(see below)_{{ else }}{{ format_output_value output.is_masked output.is_computed output.value output.provider_name output.formatting_attribute_name }}{{ end }} |
{{~ end ~}}
{{~ for output in global_outputs ~}}
{{~ if output.is_large_output_value ~}}

**`{{ output.name | escape_markdown_table_cell }}`:**

{{ format_output_value_large output.is_masked output.is_computed output.value }}
{{~ end ~}}
{{~ end ~}}
{{ end }}

{{ if ignore_case_changes && filtered_resource_count > 0 }}
> ℹ️ {{ filtered_resource_count }} resource{{ if filtered_resource_count > 1 }}s{{ end }} with only filtered changes (e.g. Azure resource ID casing differences) {{ if filtered_resource_count > 1 }}are{{ else }}is{{ end }} not shown. Run without `--ignore-case-changes` to see all changes.
{{ end }}
