# Documentation Updates for Feature #039

## Summary

Updated user-facing documentation to describe the new Azure DevOps variable group custom template feature.

## Files Updated

### 1. README.md

**Location:** Root directory

**Changes:**
- Added "Specialized templates" bullet point to Features section mentioning Azure DevOps variable groups
- Updated "Resource-Specific Templates" section to include `azuredevops_variable_group` in the list of supported resources

**Impact:** Users browsing the main README will immediately see that variable group support is available.

### 2. docs/features.md

**Location:** Documentation directory

**Changes:**
- Added `azuredevops_variable_group` to the "Supported Resources" table in the "Resource-Specific Templates" section
- Added comprehensive "Azure DevOps Variable Groups" subsection with:
  - Overview of the problem this template solves
  - Feature list (unified display, secret protection, semantic diffing, large value handling, Key Vault integration)
  - Table column descriptions
  - Example output showing variable changes with before/after diffs
  - Key Vault integration example
  - Explanation of secret value protection

**Impact:** Users reading the detailed features documentation will understand exactly how the variable group template works and what problems it solves.

### 3. examples/azuredevops/README.md

**Location:** Examples directory

**Changes:**
- Updated the "Notes" section to reflect that when using tfplan2md, variable groups display all variable metadata including secret variables (with values shown as `(sensitive / hidden)`)
- Removed outdated note that "Secret variables... won't appear in plan outputs"

**Impact:** Users working with the Azure DevOps example will have accurate information about how variable groups are rendered.

## Documentation Quality

All changes follow the project's documentation standards:

- **User-focused:** Describes what users can do with the feature, not implementation details
- **Consistent terminology:** Uses the same language as existing documentation
- **Clear examples:** Includes markdown examples showing actual output
- **Problem-solution oriented:** Explains the problem the feature solves
- **Follows existing patterns:** Matches the structure used for other resource-specific templates (firewall rules, NSG rules)

## Verification

The documentation accurately reflects the implementation:

- ✅ All variables (regular and secret) displayed in unified table
- ✅ Secret values shown as "(sensitive / hidden)"
- ✅ Metadata for all variables visible (name, enabled, content_type, expires)
- ✅ Semantic diffing with change indicators (➕, 🔄, ❌, ⏺️)
- ✅ Large value handling for long strings
- ✅ Key Vault integration display
- ✅ Support for create/update/delete operations

## Notes

- No changes were made to CHANGELOG.md (auto-generated by Versionize)
- No internal implementation details were documented in user-facing documentation
- Examples use realistic Azure DevOps variable group scenarios
- Documentation emphasizes the security aspect (protecting secret values while showing metadata)
